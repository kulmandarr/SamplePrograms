const firstLastElementInArray = (arr) => {
  return `First element is ${arr[0]} and last element is ${arr[arr.length - 1]}`
}
const result = firstLastElementInArray(['abc',2,5,2,'pqr',4,'ashish'])
console.log(result)
